/** Nombre del evento que `<login-view>` despacha tras una credencial válida — sin detalle, `app.element.ts` solo necesita saber que ya hay sesión. */
export const LOGIN_VIEW_SUCCESS_EVENT = 'login-view-success';
