/**
 * Icono SVG (inline, `stroke="currentColor"`) de favorito — mismo estilo que
 * `cloud-sync-icons.ts`. Un único path para los dos estados (favorita/no
 * favorita): el relleno (`fill: currentColor` vs `fill: none`) se controla
 * por CSS según el estado, sin duplicar el icono.
 */

/** Estrella de favorito. */
export const FAVORITE_ICON = `<svg viewBox="0 0 24 24"><path d="M12 3.5l2.7 5.8 6.3.6-4.8 4.3 1.4 6.3-5.6-3.4-5.6 3.4 1.4-6.3-4.8-4.3 6.3-.6z"/></svg>`;
