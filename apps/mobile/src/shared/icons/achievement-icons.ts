/**
 * Icono SVG (inline, `stroke="currentColor"`) placeholder genérico para
 * todos los logros — mismo estilo sobrio de 2 colores que el resto de
 * `shared/icons/` (ver ADR-046). Personalización por logro queda para un
 * cambio futuro (ver design.md de sistema-logros, Non-Goals).
 */

/** Icono genérico de logro desbloqueado (medalla). */
export const ACHIEVEMENT_PLACEHOLDER_ICON = `<svg viewBox="0 0 24 24"><circle cx="12" cy="9" r="6"/><path d="m8.5 14 -1.5 7 5 -3 5 3 -1.5 -7"/></svg>`;
