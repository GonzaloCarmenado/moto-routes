## 1. Scaffolding de `apps/web`

- [x] 1.1 Crear `apps/web/` (estructura por dominio: `src/login/`, `src/reporting/`, `src/shared/` — mismo patrón que `apps/mobile/src/`, sin `src-tauri/`): `package.json`, `tsconfig.json` (strict), `vite.config.ts`, `index.html`.
- [x] 1.2 Test rojo + implementación: `pnpm-workspace.yaml` añade `apps/web` a `packages` (capability `monorepo-layout`, requirement modificado "El workspace de pnpm gestiona apps/mobile y apps/web") — test estructural nuevo (`apps/mobile/src/shared/ci/pnpm-workspace.spec.ts`, junto a `ci-workflow.spec.ts` por ser igualmente transversal) que confirma ambos paquetes resueltos y nombres de paquete pnpm distintos. 4/4 en verde, 1411/1411 de `apps/mobile` sin regresiones.
- [x] 1.3 `src/shared/styles/tokens.css` de `apps/mobile` duplicado en `apps/web` (no referenciado: paquetes pnpm independientes sin paquete compartido entre ambos, justificación documentada en el JSDoc de `apps/web/src/shared/base-element.ts` y en la cabecera de `tokens.css`) — mismos tokens de color OKLCH y hitbox mínima 56×56px; sin las fuentes autoalojadas (no aplica el requisito CSP offline de Tauri), usa pilas de fuentes de sistema.

## 2. Login y sesión de operador (capability `dashboard-login`)

- [x] 2.1 Test rojo: `login.service.spec.ts` — credencial correcta contra un `GET /admin/status` real (mockeado en el test) abre sesión y guarda el token en `sessionStorage`; credencial incorrecta (401) no guarda nada y devuelve el error genérico. 4/4 en verde.
- [x] 2.2 Implementación: `login.service.ts` + `login.types.ts`.
- [x] 2.3 Test rojo + implementación: `login-view.element.ts` + `.element.css` — formulario con `data-cy="login-input-token"`/`data-cy="login-button-submit"`, mensaje de error único sin distinguir el motivo del fallo. 3/3 en verde.
- [x] 2.4 Test rojo + implementación: guard de enrutado en `app.element.ts` — cualquier ruta privada sin sesión válida en `sessionStorage` redirige a `/login` sin renderizar contenido (capability `web-dashboard`, requirement "La aplicación no expone ningún contenido sin sesión válida"). 6/6 en verde (incluye login-view-success → área privada).
- [x] 2.5 Test rojo + implementación: acción de cerrar sesión (`data-cy="dashboard-button-logout"`) borra la sesión de `sessionStorage` y redirige a `/login` — cubierto en `app.element.spec.ts`.
- [x] 2.6 Test rojo + implementación: `authorized-fetch.ts` (`src/shared/http/`) — una respuesta 401 de cualquier petición autenticada invalida la sesión local y despacha `session-invalidated` (`app.element.ts` escucha y redirige a `/login`); requirement "Una sesión que deja de ser válida no expone datos parciales". 3/3 en verde. `tsc`/ESLint limpios, 16/16 Vitest de `apps/web`.

## 3. Vista de reporting (capability `reporting-dashboard-view`)

- [x] 3.1 Test rojo: `reporting.service.ts` — consume `GET /admin/status` con el token de sesión, tipa la respuesta (eventos, memoria/disco opcionales). 2/2 en verde.
- [x] 3.2 Implementación: `reporting.service.ts` + `reporting.types.ts`.
- [x] 3.3 Test rojo + implementación: `events-list.element.ts` — lista de eventos en el orden recibido (el propio endpoint ya los ordena), nivel error/warning distinguible visualmente (borde/badge vía `tokens.css`, nunca hardcodeado), estado vacío explícito (`data-cy="events-list-empty-state"`). **Todo texto de evento se inserta como `textContent`, nunca `innerHTML`** — con test de regresión explícito (mensaje con `<img onerror>` verificado como texto literal, sin crear ningún nodo `<img>`). 4/4 en verde.
- [x] 3.4 Test rojo + implementación: `host-snapshot.element.ts` — memoria/disco + timestamp cuando están disponibles; estado explícito "sin datos todavía" cuando falta cualquiera de los tres. 2/2 en verde.
- [x] 3.5 Test rojo + implementación: `reporting-view.element.ts` (contenedor nuevo, no previsto por su nombre exacto en el diseño) — orquesta carga/error/reintentar y compone `events-list`+`host-snapshot`; estado de error de red con acción de reintentar (`data-cy="reporting-button-retry"`), distinto del caso de sesión inválida (2.6, gestionado globalmente por `app.element.ts`). 3/3 en verde.
- [x] 3.6 Montado `reporting-view` (que a su vez compone `events-list`+`host-snapshot`) en el shell privado de `app.element.ts`, junto al botón de logout — guard de 2.4 ya aplicado. 27/27 Vitest de `apps/web`, cobertura 99.5% líneas, `tsc`/ESLint limpios.

## 4. Servir `apps/web` desde `apps/api`

- [x] 4.1 Nueva etapa `web-build` en `apps/api/Dockerfile` (Node 22, `npm install`+`npm run build`, sin pnpm/workspace: `apps/web` no tiene ninguna dependencia `workspace:*`, se resuelve standalone) — **decisión no prevista en el diseño**: `apps/web` se pasa como *additional build context* de BuildKit (`docker-compose.yml`/`.prod.yml`, `build.additional_contexts: web: ../../apps/web`) en vez de ampliar el contexto principal (`context: ../../apps/api`) al monorepo entero — evita arrastrar `apps/mobile` (con su propio `node_modules`/`target`) al build de la API. `apps/web/.dockerignore` nuevo (`node_modules`/`dist`/`coverage`/artefactos de Cypress) — sin él, el `node_modules` local (pnpm) choca con el instalado por `npm` dentro del contenedor. `dist/` real copiado a `internal/webui/dist` antes de `go build`. Verificado con `docker compose build api` real (`docker-api:latest`, 206MB) — build completo en verde.
- [x] 4.2 Test Go rojo + implementación: `apps/api/internal/webui` sirve `dist/` vía `embed.FS` bajo `/dashboard/*` (montado en `main.go`, separado de `/api/*` y `/admin/status`, con redirect `/dashboard` → `/dashboard/`); fallback a `index.html` para rutas del router de cliente (SPA) — verificado con petición a `/dashboard/login` (ruta que no existe como fichero). 2/2 tests Go en verde.
- [x] 4.3 Confirmado: `apps/api/internal/webui/dist/index.html` es un placeholder committeado (obligatorio — `//go:embed` falla en compilación si el directorio no existe o está vacío, no es un fallback en tiempo de ejecución). `go build ./...`/`go vet ./...`/`go test ./...` (283/283) en verde en local sin ejecutar el build de `apps/web` — el Dockerfile sobrescribe el placeholder solo dentro del contenedor.

## 5. Desarrollo local

- [x] 5.1 `apps/web/vite.config.ts`: `server.proxy` reenvía `/api` y `/admin` a `localhost:8080` (override vía `WEB_DEV_API_PROXY_TARGET`) — sin CORS en ningún entorno (design.md, Decisión 1). Verificado con el dev server real: `/admin/status` sin backend arriba devuelve 500 de proxy (no un 404 estático de Vite), confirma que reenvía de verdad.
- [x] 5.2 Documentado en `documentacion/11-inicializacion-local.md` (sección nueva "4b", comandos de test/lint/build en la sección 5) y `documentacion/05-backend-api-go.md` (filas `/admin/status`/`/dashboard/*` en la tabla de endpoints, antes ausente para `/admin/status`; entrada `webui/` en el árbol de `internal/`).

## 6. CI

- [x] 6.1 Nuevo job `quality-web` en `.github/workflows/ci.yml` (tsc, ESLint, Vitest, build) apuntando a `apps/web` — mismo patrón que `quality-ts`, sin acoplarlo a rutas de `apps/mobile`.
- [x] 6.2 Cypress nuevo (`apps/web`) contra `apps/api` real (`docker compose up --build`): login con credencial correcta/incorrecta, vista de reporting con eventos reales insertados en la base/registro de eventos, logout, expiración de sesión (401 simulado) — mismo patrón de "backend real, sin mocks" ya usado en el resto del proyecto. Confirmado con `pnpm run test:e2e` real contra el stack Docker levantado (`docker compose up -d --build`): **5/5 en verde** (`sin sesión: redirige a /login`, `credencial incorrecta`, `credencial correcta: estados vacíos reales`, `cerrar sesión`, `sesión inválida por 401 simulado`).
- [x] 6.3 `quality-web` cableado en el mismo trigger que `quality-ts`/`quality-tauri` (push a `master`, pull request) — mismo `needs: [detect-doc-only-change]`, mismo bloque `on:` del workflow completo (`ci-workflow.spec.ts` ya lo cubre, 41/41).

## 7. Verificación end-to-end real

- [x] 7.1 `docker compose up --build` local: login real con el `ADMIN_STATUS_TOKEN` de `.env` local, vista de reporting mostrando eventos y métricas reales generados por la propia API. Docker Desktop estaba parado al retomar la sesión (reinicio de máquina) — arrancado, `docker compose up -d --build` real (`docker-api:latest`), `api/ping` y `/dashboard/` ambos 200. Generado un evento real (no simulado): webhook de Resend firmado de verdad (HMAC-SHA256 Svix con `RESEND_WEBHOOK_SECRET` local) contra `POST /api/webhooks/resend` con `email.bounced` — `GET /admin/status` devuelve el evento real (`email_delivery_failure`, `to: test@example.invalid`). Métricas de host quedan en estado vacío por diseño (`SYSMETRICS_PATH` sin fijar en local usa el valor por defecto documentado en `.env.example`, no aplica en este entorno). Verificación del navegador (Claude in Chrome) no disponible en esta sesión (extensión desconectada) — el renderizado del evento real vía `textContent` ya está cubierto por el test de regresión de 3.3.
- [x] 7.2 Verificar en producción real, desde la rama sin fusionar (mismo patrón que despliegues anteriores — imagen anterior etiquetada para rollback): `apps/web` accesible en `https://debian.taildf3dab.ts.net/dashboard/` (o la ruta elegida en 4.2), login con el `ADMIN_STATUS_TOKEN` real de producción, datos reales visibles. `docker compose -f infra/docker/docker-compose.prod.yml up -d --build` real completado en el servidor (commit `cdbe4e9`, imagen `docker-api:pre-dashboard-reporting` sigue disponible para rollback). Verificado por API (equivalente exacto a lo que hace el login del panel): `/api/ping` 200, `/dashboard/` 200, `GET /admin/status` con el `ADMIN_STATUS_TOKEN` real de `.env.prod` → 200 con datos reales (evento real `degraded_feature`: `FCM_SERVICE_ACCOUNT_JSON not set`), sin token → 401. Verificación visual en navegador no realizada: Claude-in-Chrome seguía desconectado esta sesión, y aun estando disponible no correspondía teclear el token real en un campo de formulario (regla de credenciales).

## 8. Cierre

- [x] 8.1 `openspec validate --all --strict` limpio (32/32).
- [x] 8.2 ADR-060 en `memory/decisions.md`: mismo origen sin CORS (embed.FS servido por `apps/api`) + credencial reutilizada en `sessionStorage` sin backend de sesión propio — decisiones de arquitectura reales con alternativas descartadas (ver design.md, Decisiones 1 y 2).
- [x] 8.3 Actualizado `memory/context.md` (estado actual, próximo hito, sección "Estructura del Proyecto" sincronizada con `apps/web`/`internal/webui`).
- [x] 8.4 Suite completa en verde antes de abrir el PR: `tsc --noEmit`/ESLint limpios y Vitest en verde en `apps/mobile` (1417/1417) y `apps/web` (27/27, más `npm run build` real), `go vet`/`go test` en `apps/api` (283/283) — `gofmt -l` marca todo el árbol por diferencia de final de línea (`core.autocrlf=true` en este checkout Windows, CRLF vs LF), falso positivo del entorno, no del código, no bloquea. Cypress completo (no solo lo nuevo): `apps/web` 5/5, `apps/mobile` 101/101, ambos contra el stack Docker local real (`docker compose up -d --build`, Docker Desktop se reinició para esta sesión).
